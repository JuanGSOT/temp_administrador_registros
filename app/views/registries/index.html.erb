<div class="grid-comp">
    <%= render "layouts/headerDate" %>
    <%= render "layouts/lateral" %>
    <div class="container main"><br>
        <h2>Sistema de prestamo del material escolar</h2>
        <h2 class="ml-5">Departamento de Desarrollo Academico</h2>
        <br>
        <h2>>>Material Prestado</h2>
        <br>
        <div class="container row">
        <% @registry.each do |i| %>
        <div class="col-xs-12 col-sm-12 col-md-6 col-lg-4">
            <div class="card my-2">
                <div class="card-header ch-registry">
                    Salon <%= i.classroom.name %> + <%= i.article.name %>
                </div>
                <div class="card-body d-flex flex-column">
                    
                    <strong>Profesor: </strong>
                    <span style="text-indent: 1em"><%= ("#{i.teacher.surnames} #{i.teacher.name}").upcase %></span>
                    <strong>Solicitado a las:</strong>
                    <span style="text-indent: 1em"><%= i.created_at.strftime('%I:%M %P') %></span>
                    <div class="text-muted my-2" style="font-size: smaller">
                        <strong>Departamento de:</strong> <br>
                        <%= i.teacher.department.name %>
                    </div>
                    <%= link_to raw("<i class='mi md-18'>&#xE163;</i> Registrar devolución"), i, method: :patch, class: "btn btn-success" %>
                    
                </div>
            </div>
        </div>
        <% end %>
        <% if @registry.blank? %>
        <div class="d-flex w-100 justify-content-center">
            <div class="card text-white bg-danger mb-3" style="max-width: 20rem;">
                <div class="card-header">Advertencia</div>
                <div class="card-body">
                    <h4 class="card-title">Sin registros actualmente</h4>
                    <p class="card-text">Por el momento no se ha registrado el prestamo de un nuevo producto.</p>
                </div>
            </div>
        </div>
        <% end %>
        
        </div>
    </div>
    <%= render 'layouts/footer' %>
</div>
