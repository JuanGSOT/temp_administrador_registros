<%= form_for @department_find, url: {action: "update"} do |f| %>
    <div class="form-group">
        <label for="name">Nombre</label>
        <%= f.text_field :name, placeholder: "nombre del departamento", class: "form-control", value: @department_find.name %>
    </div>
    <div class="form-group">
        <label for="apartment_manager">Encargado del departamento:</label>
        <%= f.text_field :apartment_manager, placeholder: "ingrese nombre completo", class: "form-control", value: @department_find.apartment_manager %>
        <small class="form-text text-muted">Ingrese este campo adecuadamente para los reportes, por ejemplo podria ser Ing. Jardines Jardines Jose.</small>
    </div>
    <%= f.submit "Guardar", class: "btn btn-success" %>
<% end %>